commit-msg:
  commands:
    lint-commit:
      run: COMMIT_MSG_FILE={1} go-task commit-msg
pre-commit:
  commands:
    check:
      run: go-task check
    test:
      run: go-task test
    lint:
      run: go-task lint
